<form method="post" action="{{ path('profil_club_delete', {'id': profil_club.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_club.id) }}">
    <button class="btn">Delete</button>
</form>
