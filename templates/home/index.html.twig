{% extends 'base.html.twig' %}

{% block title %}Gruppetto{% endblock %}

{% block body %}
    <section class="container">
        <section id="auth-buttons">
            <button onclick='location.href="{{path('app_register')}}"'  class="auth-button">Inscription</button>
            <button class="auth-button">Connexion</button>
        </section>

    </section>
    {% include 'registration/_mainLogo.html.twig' %}

    <section class="info-banner">
<p><img src="{{ asset('build/images/icons8-running-1.svg') }}" alt="run logo">Crée tes propres événements</p>
        <hr>
<p id="middle-p"><img src="{{ asset('build/images/icons8-running-1.svg') }}" alt="run logo">Gère ton club</p>
        <hr>
<p><img src="{{ asset('build/images/icons8-running-1.svg') }}" alt="run logo">Crée tes propres événements</p>
    </section>
{% endblock %}
